import PostAdForm from "@/components/website/post-ad/PostAdForm";

export const metadata = {
  title: "Post Your Ad | Errand Hub",
  description: "Promote your business in our directory.",
};

export default function PostAdPage() {
  return <PostAdForm />;
}
