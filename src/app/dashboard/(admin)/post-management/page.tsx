import PostManagementPage from "@/components/dashboard/admin/postManagement/PostManagementPage";

export const metadata = {
  title: "Post Management | Errand Hub",
  description: "Manage errands and posts on Errand Hub",
};

const page = () => {
  return <PostManagementPage />;
};

export default page;
